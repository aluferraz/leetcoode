/**
 * @param {string} s
 * @return {string}
 */


//Unfinished
var longestDupSubstring = function (s) {
    let left = 0;
    let right = s.length - 1;
    while (left < right) {
        let mid = Math.floor(left + right / 2);

    }
};
function getSubstrings(s, len) {
    let left = 0;
    let right = len;
    while(right - s.len){
        
    }
}


console.log(longestDupSubstring("banana"))