/**
 * @param {number} n
 * @return {number}
 */
var nextGreaterElement = function (n) {
    return -1;
};

function swap(i, j, array) {
    let temp = array[i];
    array[i] = array[j];
    array[j] = temp;
}
nextGreaterElement(230241);